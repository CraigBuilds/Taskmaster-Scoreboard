name: taskmaster_server
description: Backend server for the Taskmaster Scoreboard app.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_web_socket: ^1.0.4
  shelf_static: ^1.1.0
  web_socket_channel: ^2.4.0
  args: ^2.4.0
